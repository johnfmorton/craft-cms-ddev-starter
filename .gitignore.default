/.env
/.idea
/vendor
.DS_Store

#Craft storage
!/storage/
/storage/**/storage/*
!*/storage/.gitkeep

# Build files
node_modules/*

# MISC FILES
.cache

# Database seed files
db-seed/*.sql
db-seed/*.sql.zip
!db-seed/.gitkeep
!db-seed/starter-v*.sql.zip

# ignore *.crt and *.key files
*.crt
*.key

# If you're building locally and then pushing to the server, you may want the dist folder in your repo
# if your build process is on the server, you may want to ignore it
#/web/dist
